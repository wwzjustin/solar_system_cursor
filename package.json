{
  "name": "solar-system",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist -b gh-pages"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "three": "^0.177.0"
  }
}
